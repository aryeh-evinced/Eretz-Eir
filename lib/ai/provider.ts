/**
 * Call AI provider with Claude as primary and OpenAI as fallback.
 * Stub — implementation in Phase 5.
 */
export async function callAI(_prompt: string, _systemPrompt: string): Promise<string> {
  throw new Error("Not implemented — requires AI provider configuration (Phase 5)");
}
